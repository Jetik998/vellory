location / {
    error_page 401 /login;
    try_files /templates/index.html =404;
}

location /login {
    try_files /templates/login.html =404;
}

#Статические файлы
location /static/ {
    try_files $uri =404;
}

location /avatars/ {
    try_files $uri =404;
}